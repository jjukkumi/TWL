# holiday todo

- django
- flask
- 면접준비

- [django_프로젝트배포하기](https://nachwon.github.io/django-deploy-1-aws/)

## flask

- SQL, NoSQL?

```
SQL 데이터베이스는 효율적이고 콤팩트한 형태에서 구조화된 데이터를
저장할때 성능을 발휘하며 일관성을 유지하는 데 효과적이다.
반면에 NoSQL데이터베이스는 일관성 유지 측면에서 다소 약한 편이여서
결국 성능 한계에 부딪히기도 한다.

sql의 장점
명확하게 정의 된 스키마, 데이터 무결성 보장
관계는 각 데이터를 중복없이 한번만 저장

nosql의 장점
스키마가 없기때문에, 훨씬 더 유연. 즉, 언제든지 저장된 데이터를 조정하고 새로운 "필드'를 추가 할 수 있습니다.

데이터는 애플리케이션이 필요로 하는 형식으로 저장됨, 이렇게 하면 데이터를 읽어오는 속도가 빠름

수직 및 수평확장이 가능 데이터베이스가 애플리케이션에
발생기키는 모든 읽기/쓰기 요청을 처리할 수 있다.


```

- sqlalchemy

```

```


## 면접준비

- 요즘 관심있게 보는 기술은?

```
(aws lambda, GCP cloudrun, kubernetes knative)
1.serverless
내가 직접 서버를 관리하지 않는 경우

아마존 EC2에 돈을 내면 최신식 서버를 빌려서 사용할 수 있었음

백앤드를 작은 함수단으로 쪼개서
내가 직접 관리하지 않는 서버로 올린다

서버리스가 아닌 경우 서버는 24시간 돌아간다.
언제나 요청에 응답할 준비를 하고 있지.

서버리스의 경우 24시간 깨있지 않음
그러나 요청이 오는 순간 AWS는 lambda를 깨울 것이고,
요청한 작업을 수행하고 다시 잠듬

24시간 동안 대기 안해도됨 저렴해짐

서버리스는 내가 수행한 만큼만 돈을 내면 됨

ex) aws lambda 매우쌈

서버리스의 단점
cold start : 요청이 와서 깨우는데 시간이 걸림
긴 시간은 아니겠지만 그래도 시간이 걸림

aws lambda 어떤 함수가 자주 쓰이는지 판단하고,
해당함수는 아예 잠들지 않고 요청에 빨리 대응할 수 있도록
대기한다.

서버 제공자에 너무 의지하게된다.:
aws에 배포했다면 aws에서 서버를 빌렸다가 GCP로 가는 것은
쉽지만 서버리스는 이동하기 쉽지않다.

어떻게 설계하고 작업했는지 익숙해지면, 이후에는 잘 될 것

누가 서버리스를 쓰는게 좋나?
사이드 프로젝트
최대한 빠르게 프로토타입을 출시하고 싶은 경우

서버리스로 작업하면 코드에만 집중하면 되고, 설정은 신경 꺼도됨

aws cognito(코그니토)
auth를 서비스로 사용 로그인,로그아웃 이런거 안짜도 됨

2. NextJS
기업용 리액트 앱을 만들기 위한 프레임워크
기업이 좋아할만한 기능
서버사이드렌더링, SEO ,타입스크립트, PWA내보내기기능,
static최적화, 서버리스로 배포

3. 개츠비.js
리액트를 활용해서 static웹을 만들 수 있는 프레임워크
static웹 다이내믹 데이터를 다루지 않는 심플한 웹

4. flutter
구글이 만든 크로스플랫폼(ios/android)앱 제작 툴
DART언어를 사용

대기업들이 사용하기 시작함
도미노 효과
구글 stadia(게임 플랫폼)가 이거씀

5. web assembly

go,kotlin,rust같은 언어들을 브라우저가 이해할 수 있는
언어로 컴파일 해준다.

웹 어셈블리는 C나 C++과 같은 프로그래밍 언어를 컴파일 해서
어느 브라우저에서나 빠르게 실행되는 바이너리 형식으로 
바꿔주는 기술을 뜻합니다.

이 기술은 여러 언어로 작성된 코드를 네이티브에 가까운 속도로 
웹에서 실행가능하게 하는 목적과 js단점을 보완하기 위해 등장



```